{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "selection-window",
  "description": "Minimal capabilities for the selection overlay window",
  "windows": ["selection"],
  "permissions": [
    "core:default"
  ]
}
