# T9: tray.rs 内联测试

## 完成状态: DONE

## 变更摘要

### `src-tauri/src/tray.rs` 修改:

1. **提取菜单项 ID 为公共常量**:
   - `MENU_ID_SCREENSHOT`, `MENU_ID_HISTORY`, `MENU_ID_SETTINGS`, `MENU_ID_ABOUT`, `MENU_ID_QUIT`
   - `ALL_MENU_IDS` 数组用于迭代/验证
   - 原有的硬编码字符串已替换为常量引用

2. **添加 `#[cfg(test)] mod tests` 测试模块** (6 个测试):
   - `menu_ids_are_correct`: 验证每个常量值正确
   - `all_menu_ids_contains_five_items`: 验证菜单项数量
   - `all_menu_ids_are_unique`: 验证无重复 ID
   - `all_menu_ids_cover_expected_actions`: 验证所有 ID 都是预期的
   - `menu_ids_match_event_handler_branches`: 验证常量集与 match 分支一致
   - `create_tray_function_signature_is_valid`: 编译期验证函数签名

3. **rustfmt 格式化**: 已应用

## 环境限制

`cargo test` 无法在当前 WSL 环境运行——缺少系统级依赖 (libdbus-1-dev, libgtk-3-dev, libwebkit2gtk-4.1-dev 等)。

## 测试策略说明

由于 `create_tray` 需要 Tauri `AppHandle<R>`（无法在测试中实例化），测试侧重于常量正确性、编译期签名验证、常量集与事件处理分支的一致性检查。
